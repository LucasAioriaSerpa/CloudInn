{
  "name": "cloudinn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "lint": "npm run build --workspace=frontend",
    "test": "npm run test:functions",
    "test:functions": "node --test api/functions/**/*.test.js"
  },
  "dependencies": {
    "@azure/functions": "^4.16.2",
    "mongodb": "^7.6.0"
  }
}
